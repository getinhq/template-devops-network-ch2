name: CI
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
jobs:
  compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker compose config
      - name: TESTING.md must be completed
        run: '! grep -q "TODO" TESTING.md'
